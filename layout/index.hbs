<!DOCTYPE html>
<html lang=en dir=ltr>

  <head>
    {{> head }}
  </head>

  <body>
    {{> navbar }}
    {{{ body }}}{{!-- actually <main> but express-handlebars calls this variable 'body' --}}
    {{> footer }}
    {{> sprites }}
  </body>

</html>
